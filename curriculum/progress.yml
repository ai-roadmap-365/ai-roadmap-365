# Course progress tracker — the single source of truth for completion.
# A day may carry status: complete ONLY when every one of the 13 completion
# flags below is true, and audit:coverage cross-checks this in both
# directions (requirements §13). Days absent from this file are "planned".
#
# statuses: planned | draft | in-review | complete

days:
  1:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      verify:all green; lab executed on macOS (18/18 checks, output
      captured); WordPress package generated+validated with inlined SVGs;
      release:export privacy scan passed; editorial review completed
      2026-07-12.
    completion: &done
      lesson_content: true
      lesson_rendered: true
      lab_created: true
      lab_executed: true
      tests_passed: true
      visuals_complete: true
      sources_verified: true
      readme_complete: true
      local_links_passed: true
      lab_links_generated: true
      blog_published: true
      privacy_scan_passed: true
      editorial_review_passed: true
  2:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (16 headings, 2 diagrams, 8-q quiz);
      toy-CPU lab executed on macOS (27/27 checks); instructor answer key
      verified against real traces; validators green.
    completion: *done
  3:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract; memory-hierarchy lab executed on
      macOS (19/19 checks) with real cache/RAM/read measurements captured;
      instructor solution from real values; validators green.
    completion: *done
  4:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract; binary-toolkit lab executed on macOS
      (14/14 checks); instructor answer key verified with the toolkit;
      validators green.
    completion: *done
  5:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract; byte-inspection lab executed on macOS
      (18/18 checks) with real PNG/UTF-8 samples verified by file/xxd;
      instructor key from real byte values; validators green.
    completion: *done
  6:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract; OS-exploration lab executed on macOS
      (29/29 checks); instructor solution from real values; validators green.
    completion: *done
  7:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract and closes Week 1; process-playground
      lab executed on macOS; instructor solution and rubric; validators
      green.
    completion: *done
  8:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Meet the Terminal); lab executed on macOS
      (35/35 checks); instructor solution; validators green.
    completion: *done
  9:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Navigating the Filesystem); lab executed on macOS
      (15/15 checks); instructor solution; validators green.
    completion: *done
  10:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Working with Text); lab executed on macOS
      (20/20 checks); instructor solution; validators green.
    completion: *done
  11:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Environment Variables); lab executed on macOS
      (13/13 checks); instructor solution; validators green.
    completion: *done
  12:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Shell Scripting); lab executed on macOS
      (11/11 checks); instructor solution; validators green.
    completion: *done
  13:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Package Managers); lab executed on macOS
      (14/14 checks); instructor solution; validators green.
    completion: *done
  14:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Automating Tasks with cron); lab executed on macOS
      (18/18 checks); instructor solution; validators green.
    completion: *done
  15:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (What Happens When You Load a Web Page); lab executed on macOS
      (12/12 checks); instructor solution; validators green.
    completion: *done
  16:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (IP Addresses, DNS, and Routing); lab executed on macOS
      (12/12 checks); instructor solution; validators green.
    completion: *done
  17:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (TCP, UDP, and Ports); lab executed on macOS
      (9/9 checks); instructor solution; validators green.
    completion: *done
  18:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (HTTP: Requests, Responses, and Methods); lab executed on macOS
      (7/7 checks); instructor solution; validators green.
    completion: *done
  19:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (HTTPS and TLS); lab executed on macOS
      (10/10 checks); instructor solution; validators green.
    completion: *done
  20:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (How Browsers Render); lab executed on macOS
      (21/21 checks); instructor solution; validators green.
    completion: *done
  21:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Inspecting Traffic with curl); lab executed on macOS
      (11/11 checks); instructor solution; validators green.
    completion: *done
  22:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (What an API Is); lab executed on macOS
      (17/17 checks); instructor solution; validators green.
    completion: *done
  23:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (REST Fundamentals); lab executed on macOS
      (15/15 checks); instructor solution; validators green.
    completion: *done
  24:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (JSON and Serialization); lab executed on macOS
      (7/7 checks); instructor solution; validators green.
    completion: *done
  25:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (API Authentication); lab executed on macOS
      (16/16 checks); instructor solution; validators green.
    completion: *done
  26:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Webhooks); lab executed on macOS
      (15/15 checks); instructor solution; validators green.
    completion: *done
  27:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Rate Limits, Pagination, Errors); lab executed on macOS
      (17/17 checks); instructor solution; validators green.
    completion: *done
  28:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Consuming a Public API from CLI); lab executed on macOS
      (14/14 checks); instructor solution; validators green.
    completion: *done
  29:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Why Version Control Exists); lab executed on macOS
      (12/12 checks); instructor solution; validators green.
    completion: *done
  30:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Git Fundamentals); lab executed on macOS
      (12/12 checks); instructor solution; validators green.
    completion: *done
  31:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Branching and Merging); lab executed on macOS
      (10/10 checks); instructor solution; validators green.
    completion: *done
  32:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Remotes and GitHub); lab executed on macOS
      (12/12 checks); instructor solution; validators green.
    completion: *done
  33:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Pull Requests and Code Review); lab executed on macOS
      (11/11 checks); instructor solution; validators green.
    completion: *done
  34:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Undoing Things); lab executed on macOS
      (8/8 checks); instructor solution; validators green.
    completion: *done
  35:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Git Workflows); lab executed on macOS
      (8/8 checks); instructor solution; validators green.
    completion: *done
  36:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Choosing a Code Editor); lab executed on macOS
      (12/12 checks); instructor solution; validators green.
    completion: *done
  37:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Debuggers, Linters, Formatters); lab executed on macOS
      (9/9 checks); instructor solution; validators green.
    completion: *done
  38:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Regular Expressions); lab executed on macOS
      (8/8 checks); instructor solution; validators green.
    completion: *done
  39:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Data Storage); lab executed on macOS
      (7/7 checks); instructor solution; validators green.
    completion: *done
  40:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Observability); lab executed on macOS
      (22/22 checks); instructor solution; validators green.
    completion: *done
  41:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Thinking in Automation); lab executed on macOS
      (6/6 checks); instructor solution; validators green.
    completion: *done
  42:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Section 1 Review); lab executed on macOS
      (22/22 checks); instructor solution; validators green.
    completion: *done
  43:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Installing Python & venv); Python lab executed on macOS
      python3 3.14 (11/11 checks); instructor solution; validators green.
    completion: *done
  44:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Variables and Types); Python lab executed on macOS
      python3 3.14 (12/12 checks); instructor solution; validators green.
    completion: *done
  45:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Strings and Text Processing); Python lab executed on macOS
      python3 3.14 (9/9 checks); instructor solution; validators green.
    completion: *done
  46:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Numbers, Math, Precision); Python lab executed on macOS
      python3 3.14 (9/9 checks); instructor solution; validators green.
    completion: *done
  47:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Input, Output, f-strings); Python lab executed on macOS
      python3 3.14 (19/19 checks); instructor solution; validators green.
    completion: *done
  48:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Reading Errors & Debugging); Python lab executed on macOS
      python3 3.14 (9/9 checks); instructor solution; validators green.
    completion: *done
  49:
    status: complete
    completed_on: '2026-07-12'
    evidence: >-
      Lesson passes content contract (Your First Real Program); Python lab executed on macOS
      python3 3.14 (12/12 checks); instructor solution; validators green.
    completion: *done
  50:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Conditionals and Boolean Logic, 16 H2s + 4
      hands-on H3s, 2 SVGs); Decision Engine lab executed on macOS python3 3.14
      (22/22 checks completed); instructor solution; validators green.
    completion: *done
  51:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Loops and Iteration Patterns); Iteration
      Patterns Workbench lab executed on macOS python3 3.14 (24/24 checks
      completed); instructor solution; validators green.
    completion: *done
  52:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Lists in Depth); List Toolkit lab executed
      on macOS python3 3.14 (20/20 checks completed); instructor solution;
      validators green.
    completion: *done
  53:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Dictionaries in Depth); Word Frequency &
      Records lab executed on macOS python3 3.14 (18/18 checks completed);
      instructor solution; validators green.
    completion: *done
  54:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Tuples, Sets, and Choosing a Collection);
      Choosing Collections lab executed on macOS python3 3.14 (20/20 checks
      completed); instructor solution; validators green.
    completion: *done
  55:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Comprehensions and Iterator Thinking);
      Comprehensions & Generators lab executed on macOS python3 3.14 (12/12
      checks, lazy==eager proven); instructor solution; validators green.
    completion: *done
  56:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Building a Data-Driven CLI, week 8
      capstone); Records CLI lab executed on macOS python3 3.14 (31/31 checks
      completed); instructor solution; Terminal Task Manager project + solution;
      validators green.
    completion: *done
  57:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Functions: Definition, Arguments, Return
      Values); Function Library lab executed on macOS python3 3.14 (46/46 checks
      completed, pure functions + mutable-default trap); instructor solution;
      validators green.
    completion: *done
  58:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Scope, Closures, and *args/**kwargs);
      Flexible Functions lab executed on macOS python3 3.14 (30/30 checks
      completed, variadics + closures); instructor solution; validators green.
    completion: *done
  59:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Modules, Imports, and Project Layout);
      Split into Modules lab executed on macOS python3 3.14 (26/26 checks
      completed, wordstats package); instructor solution; validators green.
    completion: *done
  60:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (A Tour of the Standard Library); Stdlib
      Toolkit lab executed on macOS python3 3.14 (19/19 checks completed,
      pathlib+Counter+datetime+json); instructor solution; validators green.
    completion: *done
  61:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Writing Readable Code); Refactor for
      Readability lab executed on macOS python3 3.14 (14/14 checks, 2 optional
      tool skips, behavior identity proven); instructor solution; validators
      green.
    completion: *done
  62:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Recursion); Recursive Thinking lab
      executed on macOS python3 3.14 (31/31 checks completed, naive vs
      lru_cache by call count); instructor solution; validators green.
    completion: *done
  63:
    status: complete
    completed_on: '2026-07-13'
    evidence: >-
      Lesson passes content contract (Designing a Small Program Well, week 9
      capstone); Design It First lab executed on macOS python3 3.14 (23/23
      checks completed, pure core + thin shell); instructor solution; Flashcard
      Study App project + solution; validators green.
    completion: *done
  64:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Reading and Writing Files, week 10
      opener); Safe File I/O Toolkit lab executed on macOS python3 3.14.0
      (28/28 checks, 0 failures); every figure in the lesson re-derived from a
      real run of examples/demo.py (70 bytes vs 62 characters, 24.82 MiB vs
      0.14 MiB peak, the captured UnicodeDecodeError, the mid-operation
      directory listing); instructor solution; WordPress export built;
      verify:all green.
    completion: *done
  65:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (CSV and JSON in the Real World);
      Wrangling Messy Data lab executed on macOS python3 3.14.0 (26/26 checks,
      0 failures) against a deliberately hostile CSV carrying a byte-order
      mark, an embedded comma, a quoted newline, doubled quotes, an empty
      field and a ragged row; from-scratch quoting parser agrees with the csv
      module on every row; every parsed value and byte count in the lesson
      re-derived from real runs; instructor solution; WordPress export built.
    completion: *done
  66:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Exceptions and Error Handling Strategy);
      Failing Well lab executed on macOS python3 3.14.0 (29/29 checks, 0
      failures, exit 0) and re-run independently at integration; every quoted
      traceback, chained exception, and retry-backoff figure captured from a
      real run with absolute paths normalised and the difference noted;
      hand-authored unwinding flow diagram added alongside the exception
      hierarchy; instructor solution; WordPress export built.
    completion: *done
  67:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Classes and Objects); Building Your
      First Classes lab executed on macOS python3 3.14.0 (28/28 checks with
      the starter unfinished, 34/34 with it completed, 0 failures) and re-run
      independently at integration; example output diffed against the captured
      expected-output with zero differences; default __repr__ addresses shown
      as 0x... with the per-run difference noted rather than faked; pydantic
      described but not demonstrated because it is genuinely not installed;
      instructor solution; WordPress export built.
    completion: *done
  69:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Dataclasses and Type Hints); From
      Boilerplate to Dataclass lab executed on macOS python3 3.14.0 (33/33
      checks, 0 failures, exit 0) and re-run independently at integration;
      mypy confirmed NOT installed, so the lesson quotes no checker output at
      all and instead proves the stored-but-not-enforced point by inspecting
      __annotations__ and dataclasses.fields() at runtime, plus a planted-bug
      demo where only one of two type errors crashes; instructor solution;
      WordPress export built.
    completion: *done
  68:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Inheritance, Composition, and Dunder
      Methods); Protocols and Hierarchies lab built and executed on macOS
      python3 3.14.0 (36/36 checks with the starter unfinished, 39/39 once
      completed, 0 failures) and re-run independently at integration; failure
      behaviour verified by deliberately breaking __iter__ and __hash__ (4
      failures, exit 1); suite additionally passes on python3 3.11.14 after
      two portability bugs in the assertions were found and fixed; two quoted
      outputs in the pre-existing lesson were wrong (a traceback missing 3.14
      caret lines, and a __hash__ result) and were corrected against real
      runs; instructor solution; WordPress export built.
    completion: *done
  70:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Modeling a Domain with Objects, week 10
      synthesis); Model the Domain lab (Northside Gym membership — deliberately
      a different domain from the lesson's expense-tracker example so the Week
      10 project stays fresh) executed on macOS python3 3.14.0 (33/33 checks
      with the starter unfinished, 46/46 once completed, 0 failures) and re-run
      independently at integration; captured test output diffs byte-identical
      against a real run; the lesson's expense-tracker core was built from its
      own sentences and executed, including the hand-checkable 1051.30 EUR
      April report and the honest float demonstration (0.1 + 0.2 drifts while
      ten 0.10s sum to exactly 1.0); SQLAlchemy and pydantic are labelled as
      not installed rather than shown with invented output; instructor
      solution; WordPress export built.
    completion: *done
  71:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Why Test, and pytest Basics — opens week
      11); Your First Real Test Suite lab executed on macOS python3 3.14.0 with
      pytest 9.1.1 (47/47 checks, 0 failures) and re-run independently at
      integration; the suite is proved to bite by breaking a line in a temp copy
      and asserting a non-zero exit; unittest and doctest alternatives shown
      with real captured runs while Hypothesis is stated as not installed rather
      than given invented output; instructor solution verified by running the
      answer key (10 passed against the fixed module, 3 failed/7 passed against
      the unrepaired starter); WordPress export built.
    completion: *done
  72:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Fixtures, Parametrization, and Test
      Design); A Suite That Scales lab executed on macOS python3 3.14.0 with
      pytest 9.1.1 (38/38 checks, 0 failures) and re-run independently at
      integration; 41 items collected from 22 test functions proving
      parametrization expanded; scope demonstration captured with session,
      module and function fixture bodies running 1/2/4 times with matching
      teardowns; deliberate-break check names the single failing parametrized
      case while the others still run; factory_boy and Hypothesis are labelled
      as not installed rather than shown with invented output; instructor
      solution; WordPress export built.
    completion: *done
  73:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Test-Driven Development); bowling-kata lab
      executed on macOS python3 3.14.0 with pytest 9.1.1 (40/40 checks, 0
      failures) and re-run independently at integration; every pytest block in
      the lesson is copied verbatim from the lab's recorded cycle captures
      rather than retyped, and the harness verifies each recorded RED really
      shows a failure and each GREEN really shows a pass; history is dated and
      hedged honestly with no study figure quoted; instructor solution walks the
      kata cycle by cycle; WordPress export built.
    completion: *done
  74:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Mocking and Testing Boundaries); Test the
      Logic, Stub the World lab executed on macOS python3 3.14.0 with pytest
      9.1.1 (49/49 checks, 0 failures) and re-run independently at integration;
      the autospec demonstration is asserted mechanically — an un-specced Mock
      accepts a misspelled method so the naive test passes while the autospec'd
      one fails; the wrong-patch-target failure and correct-target success are
      both captured; the refactored core runs from an empty directory with no
      patching, echoing Day 70's proof; no network at any point; instructor
      solution; WordPress export built.
    completion: *done
  75:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Static Typing with mypy); closes the Day
      69 honesty call — mypy 2.3.0 is now installed in the gitignored authoring
      venv, so this day quotes real checker output where Day 69 could only prove
      by runtime inspection that annotations are stored and ignored; Catch the
      Bug the Tests Missed lab executed on macOS python3 3.14.0 (33/33 checks, 0
      failures) and re-run independently at integration; the lab asserts the
      pytest suite passes on the buggy code while mypy exits non-zero with the
      expected bracketed error codes, and that adding Any makes a caught error
      disappear; a defect found at integration was fixed — the architecture
      diagram alt contained a bracketed table name, and a closing bracket
      terminates markdown image syntax early, so the alt would have rendered
      broken; instructor solution; WordPress export built.
    completion: *done
  76:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Linting and Formatting with Ruff);
      mechanizes the hand judgements of Day 61; From Messy to Clean lab executed
      on macOS python3 3.14.0 with ruff 0.15.22 and pytest 9.1.1 (37/37 checks,
      0 failures) and re-run independently at integration; the suite passes both
      before and after formatting proving behaviour is preserved, ruff check
      exits non-zero on the messy file emitting the expected rule codes, format
      --check fails on the messy file and passes on the clean one, and
      formatting is proved idempotent; the lab includes a mutable default
      argument so the linter catches a real bug rather than only cosmetics; the
      forbidden-token grep returns zero hits on this highest-risk day, achieved
      by choosing F401/F841/E501/E711/B006/SIM108/I001/UP demonstration rules
      and never enabling the fixme rule families; instructor solution; WordPress
      export built.
    completion: *done
  77:
    status: complete
    completed_on: '2026-07-19'
    evidence: >-
      Lesson passes content contract (Quality Gates for a Python Project — week
      11 capstone); One Command, Every Gate lab executed on macOS python3 3.14.0
      with pytest 9.1.1, mypy 2.3.0, ruff 0.15.22 and coverage 7.15.2 (39/39
      checks, 0 failures) and re-run independently at integration; the gate is
      proved to have teeth by five separate defects each tripping exactly one
      stage — formatting, lint (I001/F401), types (override/return-value), tests
      (cents 7801 != 7800), and coverage (88 below fail-under=95); the
      coverage-without-assertions point is proved mechanically, a module with
      zero assert statements still reporting 100%; the coverage version was
      corrected from the brief's guess to the real 7.15.2 read from the
      installed package; the CI workflow is shipped and explained as a
      reference file and stated as never executed, and no prices or free-tier
      allowances are quoted; instructor solution; WordPress export built.
    completion: *done
  78:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (HTTP in Python with requests); Talk to a
      Server lab re-executed independently at integration on macOS python3
      3.14.0 with requests 2.34.2 (58 checks, 0 failures, exit 0); editorial
      review corrected two figures presented as captured that the lab's own
      capture contradicted — the raw-socket response header block said 143
      bytes where the real value is 142, and elapsed said 0.0012s where the
      capture says 0.0010s; the "53 checks once every exercise is complete"
      figure was re-derived from the harness itself, which replaces eight
      structural checks with three behavioural ones, so 58 minus 8 plus 3 is
      correct; all 8 quiz answer indices verified correct; instructor solution;
      WordPress export built and validated.
    completion: *done
  79:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Web Scraping Responsibly); lab
      re-executed at integration (51 checks, 0 failures, exit 0) with
      beautifulsoup4 4.15.0; editorial review found the starter baseline
      recorded as "32 failed, 1 passed" in the lesson, the lab README and
      metadata.yml executed_on — a real run produces 33 failed, 1 passed, and
      the old figure was also arithmetically impossible against a 34-test
      suite; corrected in all three places after re-running. All 8 quiz answer
      indices verified correct; no test contacts any host but 127.0.0.1;
      instructor solution; WordPress export built and validated.
    completion: *done
  80:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Building CLIs with argparse); lab
      re-executed at integration (76 checks, 0 failures, exit 0); every quoted
      output reproduced at review, including the click demonstration
      re-created from the lesson's own code block and run against the
      installed click 8.4.2; all 8 quiz answer indices verified correct;
      sources.yml replaced — it carried three entries belonging to other days
      and nothing supporting this day's claims, and now cites argparse, the
      POSIX utility conventions, the GNU command-line interface standards,
      Click, Typer and PEP 389; instructor solution; WordPress export built
      and validated.
    completion: *done
  81:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Scheduling and Background Jobs); lab
      re-executed at integration (56 checks, 0 failures, exit 0). Editorial
      review found and this checkpoint fixed two real defects. First, a
      factual error stated twice — the lesson and the quiz both claimed the
      day-of-month OR day-of-week trap fires "roughly eight times" more often
      than Friday the 13th; computed over 2000-2039 the OR set fires 62.5
      times a year against 1.675, a factor of 37.3, so both now say about
      thirty-seven. Second, a machine-state-dependent check: the harness
      asserted no orphan survived by running pgrep -f "sleep 30" over the
      whole process table, which failed on this machine because an unrelated
      process matched; the supervised child is now a uniquely named symlink to
      sleep carrying the harness's own pid, so the check can only match its
      own child. All 8 quiz answer indices verified correct; sources.yml
      replaced with crontab(5), systemd.timer, sysexits, PEP 615 and PEP 495,
      which actually support the day's claims; instructor solution; WordPress
      export built and validated.
    completion: *done
  82:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (A First Web API with FastAPI); lab
      re-executed at integration (39 checks, 0 failures, exit 0). Editorial
      review drove every quoted response through TestClient and reproduced all
      of them byte for byte, including the 422 body's detail list, loc, type
      and input fields and the 500 whose body says nothing at all; the
      OpenAPI component schema listing and the 3.1.0 version were confirmed
      from the generated document; fastapi 0.139.2, uvicorn 0.51.0, pydantic
      2.13.4 and starlette 1.3.1 all verified as the installed versions; every
      historical attribution checked. All 8 quiz answer indices verified
      correct. No defects found. Instructor solution; WordPress export built
      and validated.
    completion: *done
  83:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Packaging and Distributing Python Code);
      lab re-executed at integration (87 checks, 0 failures, exit 0) with
      build 1.5.0 and setuptools 83.0.0; editorial review found zero
      mismatches between the lesson's quoted blocks and the lab tree and
      confirmed every packaging-history attribution; all 8 quiz answer indices
      verified correct; sources.yml replaced — it cited BeautifulSoup and
      requests, irrelevant here, and nothing supporting the six PEPs the
      history section names, and now cites PEP 517, 518, 621 and 427, the
      pyproject.toml specification and Semantic Versioning; publishing is
      described but never performed; instructor solution; WordPress export
      built and validated.
    completion: *done
  84:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Shipping an Automation Toolkit — week 12
      capstone); lab re-executed at integration (52 checks, 0 failures, exit
      0). Editorial review found two real defects, both fixed here. First, the
      lesson quoted "51 checks" three times and introduced it as a real
      captured line, while the lab's own metadata.yml and
      expected-output/test-run.txt both record 52 and a re-run prints 52;
      corrected to 52 and the capture regenerated. Second, an exit-code
      collision taught as correct: partial success was assigned exit 2, which
      argparse already owns for usage errors and which this very CLI emits, so
      a scheduler could not tell a partial run from a mistyped crontab line —
      and it contradicted Day 80's teaching that 2 means the command line was
      wrong. Partial success moved to 3 and the lock-held case to 75, the
      EX_TEMPFAIL code Day 81 already uses for exactly that condition; source,
      tests, harness, README, troubleshooting, captures, the flow diagram, the
      visuals description and the quiz option were all moved together, the
      codes were re-observed by running the tool, and the lesson now teaches
      the collision explicitly rather than stepping around it. All 8 quiz
      answer indices verified correct; sources.yml replaced with sysexits, RFC
      5424, the Twelve-Factor App and systemd.timer, which support what the
      day actually claims; Week 12 project and instructor solution added;
      WordPress export built and validated.
    completion: *done
  85:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Relational Databases and SQLite — the
      reader's first database, opened by making Week 12's JSON state file fail
      concretely); Your First Database lab executed on macOS python3 3.14.0
      and re-run independently at integration (44 checks, 0 failures, exit 0);
      the lab proves the database is one ordinary file by reading the literal
      "SQLite format 3" header, demonstrates dynamic typing accepting a string
      into an INTEGER column and a STRICT table rejecting the same insert, and
      asserts a hand-written Python table scan and the equivalent SELECT
      return identical rows. First day under amendment A30: the flow diagram
      is an animated SVG with a prefers-reduced-motion block, and every class,
      id and keyframes name is namespaced per file so the two inlined diagrams
      cannot collide in the export. Honesty calls recorded in the lesson and
      FIELDS.md: the sqlite3 shell and Python's module report different
      library versions on this machine, so both are quoted and the suite
      asserts only that each is readable; a page-size claim that could not be
      verified was softened to what the header on this machine actually says;
      no prices or tiers quoted. Instructor solution; WordPress export built
      and validated.
    completion: *done
  86:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (SELECT — Filtering, Sorting, and
      Aggregating, organised around the logical evaluation order rather than
      the written order); Ask the Database Questions lab executed and re-run
      independently at integration (124 checks, 0 failures, exit 0), with a
      negative control confirming the harness reports a failure and exits 1
      when one expectation is falsified. Tests check actual result values, not
      that a query ran. Honesty calls: the brief asked for the standard claim
      that a SELECT alias cannot be used in WHERE — SQLite runs it, so the
      lesson now states that standard SQL forbids it, PostgreSQL rejects it
      and SQLite permits it as a documented extension, pinned by two tests;
      two of the author's own hand-computed expectations were wrong and the
      value-checking harness caught both, which is written up in the
      instructor solution as evidence for that style of test; DuckDB and
      pandas are described without being run and the prose says so. Animated
      evaluation-flow diagram with reduced-motion support and per-file
      namespacing. Instructor solution; WordPress export built and validated.
    completion: *done
  87:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Joins and Relationships, opened from the
      three anomalies a single wide table produces); Connecting the Tables lab
      executed and re-run independently at integration (75 checks, 0 failures,
      exit 0), with a perturbation check confirming it fails when a seed value
      is changed. The lab proves PRAGMA foreign_keys is off by default by
      inserting an orphan successfully, then enabling it and capturing the real
      rejection; implements nested-loop and hash joins in Python and asserts
      they equal the SQL result. Honesty calls: a behaviour not in the brief
      was found and taught — issuing PRAGMA foreign_keys inside a transaction
      silently reads back 0, which is exactly what Python's sqlite3 sets up for
      you, verified against the cited SQLite documentation; the N+1 measurement
      contradicted the usual slogan at under 2x rather than 500x on this
      machine, so it is reported as measured, machine-specific and variable,
      with only the query counts asserted; the invented member and loan rows
      are labelled as invented in both README and security notes. Animated
      join-flow diagram with reduced-motion support. Instructor solution;
      WordPress export built and validated.
    completion: *done
  88:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Inserting, Updating, and Schema Design,
      opened on the WHERE-less UPDATE and the discipline that prevents it);
      Change the Data Safely lab executed and re-run independently at
      integration (101 checks, 0 failures, exit 0), with a negative control
      confirming exit 1 and real expected-versus-actual diagnostics. Includes a
      from-scratch migration runner tracked by PRAGMA user_version and proved
      idempotent by a second run applying nothing. Honesty calls: verification
      changed the content twice. ALTER TABLE support differs between the two
      SQLite libraries on this machine, so the test asserts each build agrees
      with its own version number rather than pinning one answer, and an
      undocumented behaviour observed in the newer library was deliberately
      not taught; and the author's own assumption that a mid-transaction
      constraint error rolls back earlier successful statements is false — the
      error undoes only the failing statement — so the harness, lesson, quiz
      and rubric were all rewritten around the verified behaviour, testing
      COMMIT, ROLLBACK and bail separately. PostgreSQL and MySQL transactional
      DDL is explicitly flagged as unverified rather than asserted. Animated
      transaction-flow diagram with reduced-motion support. Instructor
      solution; WordPress export built and validated.
    completion: *done
  89:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Indexes and Query Performance); Make It
      Fast lab executed and re-run independently at integration (40 checks, 0
      failures, exit 0). The day is built on measurement rather than assertion:
      a seeded generator builds a few hundred thousand rows, every query is
      timed repeatedly, and the tests assert the SHAPE of the result — SEARCH
      rather than SCAN, reads at least 20x faster against roughly 309x
      measured, writes at least 1.5x slower against 11.7x measured — never a
      millisecond figure, which is written up in the lab README as a
      transferable lesson about testing around measurements. Four honesty
      calls, all recorded in the lesson and FIELDS.md: ANALYZE did not change
      a plan on this evenly distributed data, so the script detects that at
      runtime and says so rather than manufacturing a flip, and no test
      asserts one; the indexed timing benefits from a warm cache, so the
      script drops the index and re-times the scan to show the comparison was
      fair; a prefix LIKE scanned despite the index, which contradicts the
      usual folklore, so the real cause and both fixes are given instead of
      the tidier claim, and the measured MULTI-INDEX OR plan likewise
      disproves "OR always defeats an index"; and the step counts from the
      pure-Python scan-versus-bisect comparison are counted rather than timed,
      because that half is reproducible where the milliseconds are not.
      Animated query-plan diagram contrasting a scan with a seek. Instructor
      solution; WordPress export built and validated.
    completion: *done
  90:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (SQLite from Python); A Real Data Layer
      lab executed and re-run independently at integration (64 checks, 0
      failures, exit 0). SQL injection is taught by demonstration rather than
      warning — a crafted value leaks three private records and then drops a
      table against a throwaway database, and the same string bound as a
      parameter is treated as an ordinary value. The lab proves rollback on
      both a SQL error and a Python one, that PRAGMA foreign_keys is
      per-connection and a silent no-op inside a transaction, that `with
      connection:` commits a transaction but does NOT close the connection,
      and includes a mechanical ast guard that no SQL statement is built from
      parts. Environment facts were read from the interpreter rather than
      memory, including the deprecation message for the default date adapter,
      quoted verbatim from a run with warnings raised as errors. SQLAlchemy,
      aiosqlite and pandas are described but not demonstrated and no output is
      claimed for them; the bulk-insert and memory figures are single real
      measurements presented as shapes, with FIELDS.md recording what may
      legitimately differ. Animated parameter-binding diagram. Instructor
      solution; WordPress export built and validated.
    completion: *done
  91:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Designing and Querying a Real Schema —
      the Week 13 capstone); From Requirements to Report lab executed and
      re-run independently at integration (72 checks, 0 failures, exit 0). The
      day teaches the process rather than a topic list: a prose brief becomes
      entities, a logical schema, a physical schema with constraints and
      indexes, ten queries and a printed report. Its sharpest demonstration is
      a stored queue position breaking silently in three ordinary statements
      with no error raised, which is the argument for deriving it with
      ROW_NUMBER instead. Soft delete is priced honestly: the same filter is
      applied in two questions and deliberately omitted in a third, because
      which filter applies is a property of the question rather than of the
      table. The report takes its instant as a parameter with a default rather
      than reading the clock, which is what makes its output reproducible and
      testable. Money is integer pence throughout with the division by 100 at
      the display edge only. PostgreSQL is described as having real date,
      boolean and enum types but nothing was run against it and no
      version-specific behaviour is claimed. All seed people, emails, dates
      and fines are invented and said to be invented. Animated
      requirements-to-report diagram. Instructor solution written at
      integration; WordPress export built and validated.
    completion: *done
  92:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Beyond Tables — NoSQL and Key-Value
      Stores); One Domain, Four Shapes lab executed and re-run independently at
      integration (67 checks, 0 failures, exit 0), and proved able to fail
      twice — once on a real harness bug found while authoring, once
      deliberately on a throwaway copy with the document store indexing the
      wrong expression. The day models one domain in four shapes and only
      claims what it ran: dbm, SQLite's JSON functions and a from-scratch
      document store are demonstrated live, while Redis, MongoDB, Cassandra,
      DynamoDB, Neo4j, CouchDB, HBase and Memcached are described from their
      documentation with no output reproduced, stated explicitly in the
      lesson, the lab README and FIELDS.md. Honesty calls: a first draft
      asserting specific licences for those products was replaced with a
      pointer to each project's current terms, because the permitted sources
      did not carry them and licensing in that space has churned; founding
      years were omitted for the same reason rather than written from memory;
      the SQLite JSON claim was verified against the project's own changes
      page (built-ins and the arrow operators arrived in 3.38.0, 2022-02-22);
      and CAP was verified against the cited source before writing, including
      Brewer's own 2012 correction of the "pick two of three" summary. The
      index speed-up measured 88x on one run and 95x on a repeat, so both are
      printed and the test asserts a 5x floor plus the SCAN-to-SEARCH plan
      change rather than a millisecond. A real finding became a numbered lab
      step and a quiz question: an index on json_extract(body, '$.shelf') does
      NOT serve a query written with the arrow operator, and the plan stays
      SCAN. Animated partition-choice diagram drawing CAP as the operational
      choice it is. Instructor solution.
    completion: *done
  94:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Data Validation with pydantic); Guard the
      Boundary lab executed and re-run independently at integration (62 checks,
      0 failures, exit 0) with pydantic 2.13.4 and pytest 9.1.1; the gate run
      reads 12 records, accepts 4 and rejects 8 without raising, which is the
      day's central pattern — a bad record must not kill a pipeline run. The
      lesson frames validation as the third member of a family the reader
      already knows: the type checker before it runs (Day 75), the validator
      as data arrives (today), the database constraint as data is stored (Day
      88), stating what each layer catches that the others cannot. Tests
      assert on a ValidationError's `type` and `loc` and never on `msg`,
      because the message prose is not a stable API — the same argument Day 82
      made about a 422 body. Built from scratch first: a validator over
      __annotations__ that collects ALL errors rather than raising on the
      first, which is what makes the pydantic version legible afterwards.
      pydantic-settings is a separate distribution and is deliberately not
      installed, so it is described with no output reproduced; the same
      discipline covers attrs, marshmallow, cerberus and jsonschema. All
      fixture data is obviously invented and said to be invented. Animated
      validation-flow diagram showing good records coerced through and a bad
      one stopped with its real error entry. Instructor solution.
    completion: *done
  93:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (ORMs and SQLAlchemy); See What the ORM
      Does lab executed and re-run independently at integration (87 checks, 0
      failures, exit 0) against SQLAlchemy 2.0.51, and proved able to fail —
      removing the selectinload reports 2 failures and exits non-zero, caught
      both by the query-count assertion and by the capture diff. The day never
      takes the ORM's word for anything: a query-counting listener records
      every statement emitted and the tests assert on counts rather than
      timings, which is written up as a transferable testing lesson. A minimal
      ORM with its own identity map is built from scratch first, so the
      Session's vocabulary is familiar before SQLAlchemy is opened. Three
      honesty calls, each of which changed the content. First, the brief's
      claim that a bulk operation is dramatically fewer statements through
      Core than through the ORM is FALSE on 2.0.51 — add_all plus one flush
      and Core insert both cost exactly one cursor execution for 500 rows,
      because the unit of work batches into an executemany; the real 500-to-1
      gap is whether the flush sits inside the loop. The lab now tracks cursor
      executions separately from parameter sets, and finds where Core does win
      — a bulk UPDATE, where the ORM must SELECT first and builds 1013 Python
      objects against Core's none. The instructor notes carry an explicit
      instruction not to "correct" this back toward the folklore. Second, the
      flush emitted an UPDATE before an INSERT, because the unit of work sorts
      by table dependency rather than by the order of edits; the real sequence
      is captured and the animated diagram shows it, with the actual guarantee
      stated rather than the assumed one. Third, Alembic is not installed and
      no output is reproduced — and section 1 of the harness asserts it is
      still absent, so the honesty note fails loudly rather than going stale.
      A bash 3.2 parsing bug found while authoring is fixed and documented
      inline. Instructor solution.
    completion: *done
  95:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Dates, Times, and Time Zones); The Hour
      That Happened Twice lab executed and re-run independently at integration
      (75 checks, 0 failures, exit 0), and proved non-vacuous — an earlier
      iteration genuinely reported 4 failures, and the suite carries two
      built-in negative checks that fail if the resolver's transition dates are
      moved or a comparison operator is swapped. Environment verified rather
      than assumed: 598 zones, TZPATH beginning at /usr/share/zoneinfo, and
      IANA database 2026c. The day's central facts were measured, and
      measurement corrected the draft three times. A claim that a wall-clock
      stopwatch reports a negative duration was false for the scenario as
      written, so a scenario that genuinely produces -1 day, 23:20:00 was
      added and the prose rewritten to match all four measured rows; Lord
      Howe's short day was asserted at 23.5 hours before being measured, so it
      was measured; and a claim that timedelta arithmetic keeps the old offset
      is wrong — it adds to the wall fields and re-derives the offset — so it
      was corrected to what the output shows. A fact the brief never mentioned
      became a quiz question, a glossary entry and a lab assertion: two aware
      datetimes sharing a tzinfo compare equal across a fold, because they are
      compared by wall-clock fields with fold ignored. The British
      year-round-summer-time period is discovered by bisecting the zone
      database at run time rather than quoted from memory. Python 3.14's
      %Z behaviour was captured rather than guessed: it parses only UTC and
      GMT here, rejects BST and EST, and returns a naive datetime even on
      success. pytz, dateutil, arrow and pendulum are described with no output
      reproduced. No test reads a clock for a value it asserts on, and the
      suite checks that property itself. Animated DST-transition diagram.
      Instructor solution.
    completion: *done
  96:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Concurrency and async Basics); Waiting
      Versus Computing lab executed and re-run independently at integration (58
      checks, 0 failures, exit 0), and proved able to fail — removing the lock
      from the guarded counter makes the suite report that the locked counter
      lost increments, and exit 1. The day is built on measurement: the same
      workloads are run sequentially, with threads, with asyncio and with
      processes, and the tests assert the SHAPE (threads help waiting work,
      threads do not help computing work, processes do) rather than any
      duration. Honesty calls, several of which improved the day. The classic
      unprotected-counter race would NOT reproduce: eight threads and 50,000
      increments each lost exactly zero across 20 dedicated trials at the
      default 5ms switch interval on CPython 3.14.0. Rather than fake it, the
      lab reports that result first, then lowers sys.setswitchinterval to 1
      microsecond where the same unchanged code loses about 70% every run —
      and the lesson turns this into its strongest point, that a concurrency
      bug's visibility is a property of timing rather than of correctness, so
      you cannot test your way to confidence. Py_GIL_DISABLED was read from the
      interpreter (0 here) and every GIL claim is scoped to that build, with
      the suite skipping rather than failing the threads-do-not-help check on a
      free-threaded build. asyncio on CPU-bound work measured 0.98x, marginally
      slower than sequential, and is reported as that rather than rounded to
      "about the same". Two real bugs found while authoring were kept as
      teaching material: create_task schedules without starting, and a
      "fixed" deadlock demo that deadlocked on its own barrier. One vacuous
      check that always printed yes was deleted and replaced with a real
      assertion. trio, anyio, gevent and Celery are documentation-only.
      Animated event-loop diagram. Instructor solution.
    completion: *done
  97:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Logging and Configuration); Say It Where
      Someone Will Read It lab executed and re-run independently at
      integration (86 checks, 0 failures, exit 0). The day teaches the
      logging module's architecture rather than its API surface — loggers,
      handlers, formatters and filters as four separate objects, which is what
      explains the two behaviours that confuse everyone: a message that
      vanishes because the logger's level and the handler's level both have to
      pass, and a message that appears twice because of propagation. Both are
      reproduced and then fixed. The configuration half implements the four
      layers once, properly, with the provenance of every value printable, and
      proves a missing environment variable is distinguishable from an empty
      one. A redacting filter is built and the test asserts the secret's value
      appears nowhere in the captured log. Logs are captured through a buffer
      handler rather than by scraping stdout, so the assertions are on parsed
      structure. structlog, loguru, python-json-logger, pydantic-settings,
      python-dotenv and dynaconf are described with no output reproduced.
      Animated config-precedence diagram. Instructor solution.
    completion: *done
  98:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Section Project — A Complete Data
      Pipeline, the Course02 capstone); The Whole Pipeline lab executed and
      re-run independently at integration (84 checks, 0 failures, exit 0),
      stable across four consecutive runs. The day assembles Course02 into one
      offline pipeline whose five stages each carry a stated promise — ingest
      with a timeout and selective retry, a validation gate that collects and
      counts bad records rather than dying on the first, an idempotent store,
      a report taken at a parameterised instant, and one structured log line
      per stage with an exit code a scheduler can act on. Running it twice
      stores once, and that is asserted. Honesty calls: the starter's strict
      warning filter found a real defect in the author's own code — an
      HTTPError is a file object, and reading its body without closing it
      leaks a socket — which was fixed in both the reference and the starter
      and written up as a found defect rather than a planned teaching point; a
      flaky byte-comparison against pytest's reported duration was fixed in
      the HARNESS with a normaliser rather than by weakening the assertion,
      matching what FIELDS.md already promised; requests is deliberately not
      used despite Day 78, because one GET with a timeout and one header is
      what urllib.request already does, with the conditions under which
      requests earns its place stated plainly; Airflow, Dagster, Prefect and
      dbt are documentation-only and section 1 of the harness asserts none is
      importable so the claim cannot go stale; and exit code 0 is unreachable
      with these fixtures because one source always fails, which is flagged in
      the rubric rather than left as a gap. Animated pipeline-run diagram.
      Instructor solution.
    completion: *done
  100:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Matrices and What They Represent — the
      100th day, and the second of Course03); The Same Numbers, Three Ways lab
      built first and executed, then re-run independently at integration (41
      checks, 0 failures, exit 0; pytest examples 41 passed; the untouched
      starter reports 1 passed and 32 skipped, and 33 passed against a solved
      copy). The day's organising idea is that a matrix is three things — a
      table of data, a collection of vectors, and a transformation — and that
      most confusion comes from not knowing which meaning is in play; the
      static diagram draws the same 3x4 matrix all three ways. Honesty calls,
      several of which changed the content. A real false-pass bug was found
      and fixed: examples/ and starter/ both contain matrix.py, so running
      bare pytest made eleven unwritten starter exercises PASS against the
      reference solution — conftest.py in each directory plus a harness check
      asserting the skip count across a combined run now prevents it, and a
      troubleshooting claim that this produced a collection error was rewritten
      because that is not what actually happens. An assertion that `.base is
      None` proves independence failed on a real run — a copy's reshape has an
      anonymous base — and rather than delete it the discovery became lab,
      lesson, glossary and quiz material; verified independently at
      integration. A fabricated sparse-matrix threshold ("60% zeros") was
      removed and replaced with a qualitative statement plus "measured rather
      than guessed". Historical dates were fetched through the MediaWiki
      section endpoint rather than recalled, and NumPy's own origin dates were
      omitted because no permitted source states them. Every code block in the
      lesson is a byte-exact excerpt from expected-output: two paraphrased
      blocks were replaced with the literal captures. PyTorch, JAX, pandas and
      SciPy sparse are described with no output reproduced. Animated
      broadcasting diagram. Instructor solution.
    completion: *done
  99:
    status: complete
    completed_on: '2026-08-16'
    evidence: >-
      Lesson passes content contract (Vectors — Direction, Magnitude, and
      Meaning; the first day of Course03 and the first mathematics in the
      course); Vectors You Can Hold lab executed and re-run independently at
      integration (87 checks, 0 failures, exit 0) with numpy 2.5.2. The day is
      written for a reader who has done 98 days of programming and no
      mathematics beyond school arithmetic, so it opens with a row of numbers
      they have already been using rather than a definition, and every worked
      number is re-derivable with a pen — the squares, the sum and the root
      are all shown, and the exact 3-4-5 case is used where an exact answer
      teaches better than a realistic one. Nine operations are built in pure
      Python against numbered exercises and each is asserted against NumPy on
      the same inputs, so agreement is demonstrated rather than promised. The
      day ends where the course's second half begins: six invented articles
      with four hand-made numbers each, and a pairwise distance table in which
      the nearest pair falls out of the arithmetic — making "similar things
      are near each other" a computable claim. Two secondary threads are
      load-bearing and recorded as such in the lesson README: normalising the
      zero vector is undefined and raises rather than returning a plausible
      wrong answer, and a normalised vector's magnitude is not exactly 1.0, so
      every float assertion uses a stated tolerance — Day 46 being paid off
      rather than defensive coding. A case where the L1 and L2 norms rank the
      same two candidates in opposite orders is computed and explained as a
      modelling choice rather than one norm being correct. PyTorch, JAX and
      pandas are described with no output reproduced. Animated
      vector-operations diagram. Instructor solution and lesson README written
      at integration.
    completion: *done
  101:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Matrix Multiplication); Multiply It
      Yourself lab executed and re-run independently at integration (58 checks,
      0 failures, exit 0) with numpy 2.5.2. The day refuses to present the
      multiplication rule as arbitrary: matrix-vector multiplication is taught
      as a linear combination of the matrix's columns, matrix-matrix as doing
      that per column, and the shape rule is derived from what the operation
      does rather than memorised — the inner dimensions match because the
      second thing must accept what the first produces. Non-commutativity is
      shown with a concrete pair where both products are defined and
      different, computed in full. The payoff is one neural-network layer as
      X @ W + b with a bias broadcast across rows, computed by hand with small
      numbers, so the reader has done on paper the operation that dominates
      every model they will train. The elementwise-versus-matrix trap is met
      deliberately: * and @ are proved to differ on the same operands, with
      exact shapes and values asserted. Timing compares the nested-loop
      implementation against NumPy at a size where the gap is unmistakable,
      and the test asserts only a wide margin rather than any duration.
      Day 100's false-pass lesson was carried forward: conftest.py in both
      examples/ and starter/, with a harness check that the skip count is
      unchanged in a combined run. Animated composition diagram. Instructor
      solution.
    completion: *done
  102:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Linear Transformations); Where Do the
      Basis Vectors Land lab executed and re-run independently at integration
      (64 checks, 0 failures, exit 0). The day's single organising fact is that
      the columns of a matrix are where the basis vectors land, so knowing
      where two arrows go tells you where everything goes — demonstrated by
      reading a matrix off a picture and a picture off a matrix. Every standard
      transformation is derived rather than given, including the rotation
      matrix from where the basis vectors travel, with sine and cosine defined
      from the unit circle for a reader meeting them for the first time.
      Linearity is TESTED rather than asserted: it holds for a matrix
      transform and fails for a function that adds a constant, both asserted —
      which is the concrete reason a network keeps its bias separate and needs
      a non-linear activation. The determinant is introduced geometrically as
      an area factor, with the negative case as a flipped orientation and the
      zero case as a collapse nothing can undo, and the singular-matrix
      inverse asserts the exception NumPy actually raises. The day closes on
      the honest limitation that makes activation functions necessary: a
      linear transformation always fixes the origin and always maps straight
      lines to straight lines, so no stack of them can separate data needing a
      curve. Float comparisons use stated tolerances, including the honest
      note that a 90-degree rotation does not produce exactly 0.0 for cosine.
      Animated composition diagram. Instructor solution.
    completion: *done
  103:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Dot Products and Similarity); Which
      Question Are You Asking lab executed and re-run independently at
      integration (49 checks, 0 failures, exit 0; 76 reference tests pass, the
      untouched starter reports 1 passed and 51 skipped, and 52 pass against a
      solved copy). The day opens by breaking Day 99's own result — an article
      and its doubled copy are far apart under Euclidean distance and
      identical in direction — which is the honest motivation for cosine
      similarity, computed rather than asserted. Three findings changed the
      content. First, the reference suite FAILED during authoring on an
      assertion that a vector's cosine similarity with itself is exactly 1.0:
      three of the six catalogue articles miss it and one returns
      1.0000000000000002, which math.acos refuses outright — verified
      independently at integration. A synthetic example found by grid search
      was deleted in favour of the real catalogue producing it, and the
      finding is attributed to the failing test in the lesson, the lab brief
      and the instructor solution. Second, the commonly quoted sqrt(2/(pi*d))
      for the mean absolute cosine of random vectors is a large-dimension
      limit and is 14% wrong at d=2; the exact Gamma-function expression
      replaced it, reproducing the two hand-checkable cases (2/pi at d=2 and
      exactly 0.5 at d=3) and matching the measurement within 1.5% throughout,
      with both columns shown and the discrepancy discussed. Third, the second
      search query is a near-tie (0.903482 against 0.901082) and is reported as
      one, with the test asserting a margin band rather than pretending the win
      was decisive. The claim that angular distance is a metric is stated as
      known and explicitly NOT proved — one triple is not a proof — while the
      cosine-distance counter-example is labelled as a proof of the negative.
      scipy and scikit-learn are described with no output reproduced, and no
      vector-database product names or prices appear. Animated
      cosine-versus-Euclidean diagram. Instructor solution.
    completion: *done
  104:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (NumPy — Arrays and Vectorized Thinking);
      Stop Writing the Loop lab executed and re-run independently at
      integration (80 checks, 0 failures, exit 0; 107 reference tests pass, the
      untouched starter reports 1 passed and 70 skipped, and 71 pass against a
      solved copy). Three findings, two of which corrected the brief itself.
      First, the brief's memory comparison was WRONG and the wrongness became
      the lesson's opening: sys.getsizeof on a list of a million ints is
      8,000,056 against the array's 8,000,000, a ratio of 1.0000, which
      contradicts the day's own claim — the honest total, counting the 28-byte
      int objects, is 36,000,056 and 4.50x. Verified independently at
      integration. Keeping the failed measurement teaches which question was
      asked, which transfers further than the right number. Second, an
      unplanned discovery: x ** 0.5 is not np.sqrt, disagreeing on 1,390 of a
      million values, always by one unit in the last place, because IEEE-754
      requires a correctly-rounded square root while a general power routine
      has no such obligation. Reproduced exactly at integration on the quoted
      value; the tests assert the direction and a bound rather than the count,
      because the number depends on the libm Python was built against. Third,
      int8 overflow produces no warning on numpy 2.5.2, and a reference test
      asserts the ABSENCE of the warning so a future NumPy that starts warning
      fails the suite rather than letting the page go stale. No dated NumPy
      history is claimed, because none of the permitted sources carries one;
      the history section uses what is still visible in the installed software
      instead. Timings are never asserted — measured 106x to 134x, tests
      assert above 20x — and one section reports NumPy LOSING to a
      comprehension on four elements. A latent issue in Day 102's harness was
      flagged rather than silently touched. Animated vectorised-versus-loop
      diagram. Instructor solution.
    completion: *done
  105:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Transforming Images with Matrices — the
      Week 15 capstone); Rotate It Yourself lab executed and re-run
      independently at integration (79 checks, 0 failures, exit 0), and
      verified to pass both WITH a lab-local .venv present and without one,
      after the sibling-lab defect found the same day. The week's mathematics
      finally acts on something the reader can see: an image is a matrix, so
      Days 99-104 apply to it directly. The conceptual core is that you
      transform the coordinate grid rather than the pixels, and the lab makes
      the argument by doing it wrong first — forward mapping is implemented and
      the resulting holes are COUNTED and asserted, so inverse mapping is
      earned rather than announced. Homogeneous coordinates are introduced for
      the reason they exist: translation moves the origin, which Day 102 proved
      a linear map cannot do, so a third coordinate makes it a matrix multiply
      like everything else. The strongest artifact is the comparison — the
      reader's own inverse-mapping implementation with nearest-neighbour
      interpolation agrees with Pillow's Image.transform pixel-for-pixel within
      a stated tolerance, which settles the output-to-input coefficient
      convention Day 102 verified and deferred. The honest limit is stated
      plainly: these are affine transformations, so straight lines stay
      straight and parallel lines stay parallel — perspective needs a
      projective transform and lens distortion needs something non-linear. The
      test image is generated programmatically and deliberately asymmetric, so
      a flip or a transpose is visibly wrong rather than accidentally correct;
      generated files are cleaned up. Animated inverse-mapping diagram showing
      the holes and the fix. Instructor solution.
    completion: *done
  106:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Eigenvalues and Eigenvectors,
      Intuitively — the first day of Week 16); The Vectors That Keep Their
      Direction lab executed and re-run independently at integration (110
      checks, 0 failures, exit 0; 94 reference tests pass and the untouched
      starter reports 1 passed and 52 skipped). The day deliberately does not
      open with the characteristic polynomial, which is where courses lose
      people: it displays det(A - lambda I) = 0, names it as the thing that
      loses people, puts it away, and derives it later from Day 102's zero
      determinant. It opens geometrically instead, applying a matrix to a fan
      of vectors and showing which come back on their own line. The strongest
      artifact is PCA in fifteen lines — a 2-D dataset with an obvious
      elongation, its covariance matrix, and the top eigenvector shown lying
      along that elongation. Findings, all measured: numpy.linalg.eig
      CONTRADICTS ITS OWN DOCSTRING, returning complex128 for real matrices
      with real eigenvalues including the identity — verified independently at
      integration against eye(2), a real diagonal and an integer matrix — and
      the docstring is quoted exactly beside the measurement, with a starter
      exercise asking the reader to predict from the docs first so they feel
      the gap. The Rayleigh quotient's quadratic convergence is conditional
      and the lab's own matrix violates the condition: the ratio locks onto
      2.0000 for the symmetric matrix but settles at 1.0 for a matrix whose
      eigen-lines meet at 71.57 degrees rather than 90, so the measurement is
      reported first and the textbook claim restored with its condition
      attached. Diagonalising a shear fails SILENTLY — numpy.linalg.inv does
      not raise on a determinant of 2.2e-16 and returns a plausible wrong
      identity — so the lesson says to check the condition number rather than
      to expect an exception. Eigenvectors are compared by direction because
      they are only defined up to sign and scale, and FIELDS.md tells the
      reader a flipped sign is not a difference. The eig/eigh timing ratio is
      asserted nowhere; what is asserted beside it is their 1.99e-13
      agreement. SciPy, PyTorch and scikit-learn reproduce no output. The
      .venv defect fixed earlier today was verified absent here across three
      configurations, including a venv deliberately stuffed with caches.
      Animated power-method diagram. Instructor solution.
    completion: *done
  108:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Derivatives — Rates of Change; the first
      calculus in the course, for a reader with none); Watch the Slope Settle
      lab executed and re-run independently at integration (97 checks, 0
      failures, exit 0; 178 reference tests pass, the untouched starter reports
      1 passed and 99 skipped, and 100 pass against a solved copy). The day
      opens on a speedometer rather than a limit — average speed over an
      interval against the reading at an instant — and meets the limit as a
      numerical observation, computing the slope over intervals of 1, 0.1,
      0.01 and 0.001 and watching it settle before any symbol appears. The
      from-scratch artifact measures the error of the difference quotient
      across h from 1e-1 to 1e-14 and finds the U — truncation falling,
      rounding rising — with the minimum REPORTED and never asserted (measured
      1.000e-08 forward and 3.162e-06 central against balance predictions of
      2.107e-08 and 8.733e-06; the tests assert shape, band membership and
      within-10x of prediction, and FIELDS.md says why a factor of ten is
      honest rather than a hedge). Honesty calls that changed the content: the
      error curve is NOT monotone near its minimum — 1e-6 measured worse than
      both neighbours — so the shape test compares ends against middle and the
      lesson says plainly that a monotonicity test would demand something
      untrue; numpy.gradient gives two answers to one question, bit-identical
      to the from-scratch central difference with scalar spacing and differing
      with an array of the same evenly spaced coordinates, which was unplanned,
      kept and asserted both ways, and verified independently at integration; a
      first draft asserting which value frameworks use for ReLU's derivative
      at zero was REMOVED because no framework is installed, leaving only that
      the choice is a convention between 0 and 1 while the central difference
      returns 0.5, which is neither; and a privacy paragraph asserting a
      gradient-inversion research result was rewritten because it cannot be
      cited from the permitted sources. x^4 at zero is a genuine minimum the
      method cannot see, so the classifier returns undecided and the rubric
      deducts for answering "minimum" there — being right by accident is scored
      as wrong. Every tolerance is derived with headroom tabulated, and a
      reference test asserts none is loose enough to be meaningless. Animated
      shrinking-interval diagram. Instructor solution.
    completion: *done
  107:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Norms, Distances, and Similarity
      Measures); Choose Your Distance on Purpose lab executed and re-run
      independently at integration (98 checks, 0 failures, exit 0; 105
      reference tests pass, the untouched starter reports 1 passed and 71
      skipped, and 72 pass against a solved copy). The day consolidates Days 99
      and 103 into a way of CHOOSING: it opens with three measures picking
      three different winners on the same query, none of them wrong, and gives
      a decision table keyed by the shape of the data rather than by name. The
      unit "circles" of L1, L2 and L-infinity — a diamond, a circle, a square —
      are drawn on one set of axes, which is the picture that makes the family
      click. Honesty calls: the brief was WRONG in implying Mahalanobis would
      reproduce the standardised ranking, and the measurement was followed
      instead — standardised Euclidean and Mahalanobis agree that the
      unusable candidate should be demoted but disagree at the top, because
      standardising only rescales columns while Mahalanobis also removes the
      measured 0.7979 correlation, and that distinction is now taught in the
      lab, the lesson and the content README. Two correct matrix inverses gave
      two different last bits — exactly 6.0 through the lab's Gauss-Jordan and
      5.999999999999999 through numpy.linalg.inv, 8.882e-16 apart — and both
      are printed and asserted against a stated tolerance rather than one
      being declared right, which became the lab's worked argument for stating
      tolerances at all. The one randomised result asserts a RANGE (35-75%,
      measured 54.5% over 2000 seeded catalogues) because NumPy does not
      guarantee stream stability, with the exact count recorded separately and
      FIELDS.md saying which claim is durable. SciPy, scikit-learn and every
      named vector database reproduce no output. History is limited to dates
      that could be verified, with Menger's coining of "taxicab geometry"
      hedged rather than dated. Section 6 proves the harness can fail by
      re-running itself with an expectation inverted, and section 7 greps both
      implementations to confirm neither imports NumPy, so agreement with
      numpy.linalg.norm is evidence rather than a tautology. The .venv defect
      was verified absent across three configurations. Animated
      scaling-changes-the-answer diagram. Instructor solution.
    completion: *done
  109:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Partial Derivatives and Gradients);
      Which Way Is Uphill lab executed and re-run independently at integration
      (98 checks, 0 failures, exit 0; 271 reference tests pass, the untouched
      starter reports 1 passed and 205 skipped, 206 pass against a solved copy,
      and a combined run leaves the skip count unchanged so the import guard is
      checked rather than trusted). The day builds the sentence the next three
      depend on — the gradient points uphill and its magnitude is how steep
      that is — and demonstrates rather than asserts both key facts: stepping
      along the gradient beats every other direction tried around the circle,
      and the gradient is perpendicular to the contour, shown by a dot product
      near zero. Honesty calls: an assertion FAILED and the failure was kept as
      content — a plane's gradient is constant, but at (1000, -1000) the
      numerical estimate breaks the lab's own 1e-8 tolerance, so rather than
      widen the tolerance the lab asserts the breakage and tracks the roundoff
      bound across seven decades, which became a lesson section and the
      security note that a tolerance nobody checked is decoration. A test
      encoding a plausible but false belief was replaced: a finer direction
      sweep does NOT always give a smaller gap — a 60-direction and a
      360-direction sweep leave exactly the same 0.4349 degree gap because both
      grids contain 72 — so the assertion now states the law that is true, that
      the gap never exceeds half the sampling step. numpy.gradient's edge_order
      default was found by measurement and asserted in both directions so a
      future change fails loudly. Prose was corrected to match measurement
      three times rather than the reverse. A partial fix was REJECTED rather
      than shipped with a comment overstating it, and the residual issue was
      escalated: running the README's own pytest command left bytecode that
      then failed the harness — verified at integration and fixed across all
      16 affected labs. JAX, PyTorch and SymPy reproduce no output, and no real
      model's parameter count appears anywhere. Animated steepest-ascent
      diagram. Instructor solution.
    completion: *done
  110:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (The Chain Rule — the hinge of the
      calculus arc); Rates Multiply lab executed and re-run independently at
      integration (120 checks, 0 failures, exit 0, verified after running the
      README's own pytest command; 235 reference tests pass and the untouched
      starter reports 2 passed and 163 skipped against 165 fully solved). The
      day opens on gears rather than notation — if A turns twice per turn of B
      and B three times per turn of C, then A turns six times per turn of C,
      and the rates multiplied is the whole idea. Its central artifact is a
      reverse-mode autodiff engine built from scratch, a Value class recording
      its children with a backward() that walks the graph applying the chain
      rule, every gradient verified against Day 108's central difference — the
      reader writes the core of an autograd engine and checks it numerically.
      The most valuable finding corrected the brief itself: a test asserting
      that 40 stacked tanh layers vanish geometrically FAILED, because each
      tanh pulls its input toward zero where the slope returns to one, so the
      product decays polynomially rather than exponentially. Measured
      8.397332e-03 against a constant-factor prediction of 3.149274e-13, ten
      orders apart; verified independently at integration, where 40 layers
      left the input at 0.189 with a local rate of 0.965 rather than 0.42. The
      test now asserts the shape and a contrast case where a genuinely
      constant factor does collapse, and never the value. A second draft claim
      also failed: 0.5^50 does NOT vanish — it is exactly four times epsilon
      and still moves a weight — so the example moved to 0.25^50 with 0.25
      justified as the sigmoid's measured maximum slope. Two bugs in the
      author's own test expectations were caught only by solving the starter
      outside the lab. Negative zero is reported rather than normalised away,
      and float non-associativity is asserted as a real difference rather than
      compared with equality. PyTorch, JAX, TensorFlow and SymPy reproduce no
      output. The network's parameters are deliberately contrived so tanh
      returns exactly 0.5 and hand and engine can be compared with exact
      equality; the contrivance is declared openly in the dataset and the
      lesson, and was kept at integration because an exact match is a stronger
      claim than an approximate one. Animated backward-pass diagram.
      Instructor solution.
    completion: *done
  111:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Gradient Descent from Scratch — the day
      Week 16's calculus and linear algebra turn out to be one subject).
      Descent by Hand lab executed and re-run independently at integration (50
      checks, 0 failures, exit 0, harness's own exit status captured rather
      than a pipeline's); 24 reference tests pass, the untouched starter
      reports 1 passed and 20 skipped, and a solved copy kept outside the lab
      passes all 21. The closed-form claims were re-derived at integration
      outside the lab's own code: for f = a x^2 / 2 with a = 5 the per-step
      factor |1 - eta a| gives monotone decrease at eta 0.1, an exact one-step
      landing at 0.2, alternating-but-converging at 0.35 and divergence at
      0.45, and with the optimal rate 2/(1+kappa) the steps to 1e-6 run 1, 35,
      139, 691 for kappa 1, 5, 20, 100 — so the ill-conditioning gap is 691x,
      not the 5.3x an earlier scheme produced. Two measurements corrected the
      brief and are reported rather than smoothed over: that rate change, and
      momentum at beta 0.9 measuring worse than plain descent on this bowl
      while beta 0.5 won at 34 steps against 122. Divergence overflow captured
      as a real run (inf at step 3890, nan at 3891). The lab was rebuilt from
      scratch after a concurrent-writer collision, so every captured output
      comes from its own final run. scipy, PyTorch and JAX reproduce no output.
      Animated learning-rate-regimes diagram. Instructor solution.
    completion: *done
  112:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Visualizing Optimization — you cannot
      debug what you cannot see). Seeing the Descent lab executed and re-run
      independently at integration by following the README's own setup, which
      reproduced its stated versions exactly (numpy 2.5.2, Pillow 12.3.0):
      31 checks, 0 failures, exit 0 with the harness's own status captured,
      and no PNG or GIF left behind. 10 reference tests pass; the untouched
      starter reports 13 skipped and a fully solved copy 13 passed. matplotlib
      is genuinely absent and the harness asserts its import fails, so the
      from-scratch build is real: contour maps, paths, loss curves and an
      animated GIF are drawn with Pillow's ImageDraw, plus an ASCII contour
      renderer with no dependencies at all, and no matplotlib, Plotly,
      TensorBoard or Weights & Biases output is reproduced anywhere. The
      geometric convergence rate is proved from the plot's own coordinates by
      asserting the log-axis points are collinear, rather than asserted in
      prose. Two learning-rate-sweep labels were corrected against printed
      data — a diverging run that has not yet overflowed is not converging.
      Foreign files from a concurrent-writer collision were deleted rather
      than merged, and every capture regenerated afterwards. Animated
      descent-path diagram. Instructor solution.
    completion: *done
  113:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Probability: Events, Rules, and
      Intuition — the first probability in the course, opening on de Méré's
      1650s wager). Probability You Can Count lab executed and re-run
      independently at integration (57 checks, 0 failures, exit 0); 93
      reference tests pass and the untouched starter reports 3 passed and 43
      skipped, with the solved starter reaching 46 passed. The day's headline
      numbers were re-derived at integration outside the lab's own code: 1 -
      (5/6)^4 = 671/1296 = 0.5177 is favourable while 1 - (35/36)^24 = 0.4914
      is not, and the two-dice sum has P(7) = 1/6 with a most-to-least-likely
      ratio of exactly 6. Exact rational arithmetic with Fraction wherever the
      answer is rational, so those assertions cannot drift; sampled figures
      carry tolerances derived from the standard error rather than guessed, and
      FIELDS.md separates the two. The harness proves it can fail by swapping
      in a wrong assertion, confirming a non-zero exit, and restoring the file.
      scipy and pandas reproduce no output and are declared as not installed.
      The chain-rule naming collision with Day 110 is addressed head-on rather
      than left for the reader. Animated conditioning diagram. Instructor
      solution.
    completion: *done
  114:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Random Variables and Distributions — the
      step that turns outcomes into numbers you can do arithmetic on).
      Distributions You Can Sample lab executed and re-run independently at
      integration (63 checks, 0 failures, exit 0, harness's own exit status
      captured); 69 reference tests pass and the solved starter reaches 45. The
      centrepiece claims were re-derived at integration outside the lab's own
      code, by exact rational enumeration of the 36-outcome dice space with the
      first die and the sum as a deliberately dependent pair: expectation
      forgives dependence, E[X+Y] = 21/2 = E[X]+E[Y] exactly, while variance
      does not, Var[X+Y] = 175/12 against a naive 35/4, the gap closing exactly
      when the covariance term is included. Jensen's gap equals the variance
      exactly; the Binomial-to-Poisson maximum pmf gap shrinks monotonically
      across n = 10, 100, 1000, 10000 at lambda 2; and Uniform(0, 0.5) has
      density 2 with an integral of 1, which is the misconception the day
      exists to kill. The max-gap threshold between the hand-written and
      library exponential samplers is derived from the
      Dvoretzky-Kiefer-Wolfowitz inequality with the derivation shown, not
      tuned until the test passed, and was checked across five seeds for
      flakiness. The harness proves it can fail twice, once through its own
      built-in self-test and once by hand. A real bash 3.2 quote-tracking quirk
      was reproduced with a minimal case and fixed rather than worked around
      blindly. scipy, pandas and matplotlib reproduce no output. Animated
      inverse-CDF sampling diagram. Instructor solution.
    completion: *done
  115:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Bayes' Theorem — the law of total
      probability read backwards, opened on the medical test almost everyone
      gets wrong). Bayes You Can Trust lab executed and re-run independently at
      integration (53 checks, 0 failures, exit 0, harness's own exit status
      captured); 71 reference tests pass, the untouched starter reports 2 passed
      and 38 skipped, and a solved copy reaches 40. Claims re-derived at
      integration outside the lab's own code: a 99%-sensitive, 99%-specific
      test against a 1-in-1000 base rate gives a posterior of exactly 99/1098 =
      11/122 = 0.090164, not the 0.99 nearly everyone answers; the natural-
      frequency table gives 99 true positives against 999 false ones out of
      100,000; and at a prevalence of one half the same test really does give
      exactly 0.99, which is the sharpest possible demonstration of what a base
      rate does. The author's own brief carried a wrong figure — it gave
      500 x ln(0.01) as about -1151.29, which is 500 x ln(0.1); the correct
      value is -2302.585, confirmed independently at integration. The agent
      computed it, used the measured value everywhere, and flagged the
      correction in the lesson, the lab README and metadata.yml rather than
      quietly patching it. The underflow half of that claim is also asserted:
      0.01 to the 500th is exactly 0.0 in float64, which is why the classifier
      works in log space. Naive Bayes is built from scratch with Laplace
      smoothing, and removing the smoothing is asserted to drive a class
      probability to exactly zero so one unseen word vetoes every other. The
      correlated-tests case reports both the correct posterior and the higher
      one a false independence assumption produces. Harness proved able to fail
      twice, once by its own self-test and once by hand. scipy, scikit-learn
      and PyMC reproduce no output. Animated posterior-update diagram.
      Instructor solution.
    completion: *done
  116:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Descriptive Statistics That Don't Lie — a
      summary statistic is a compression, and the only professional question is
      what it hid). Statistics That Don't Lie lab executed and re-run
      independently at integration by following the README's own setup: 55
      checks, 0 failures, exit 0 with the harness's own status captured, and 32
      reference tests passing. Claims re-derived at integration outside the
      lab's own code. The 75th percentile of [1,2,3,4,6,8,9,15] takes SEVEN
      distinct values across NumPy's nine interpolation conventions — linear
      8.25, lower 8.0, higher 9.0, nearest 8.0, midpoint 8.5, weibull 8.75,
      median_unbiased 8.5833, normal_unbiased 8.5625, hazen 8.5 — so two tools
      can report different quartiles for identical data and both be right.
      Anscombe's published 1973 quartet reproduces exactly: all four sets agree
      on mean x 9.00, sample variance of x 11.00, mean y 7.50, variance of y
      about 4.12, correlation 0.816 and the fitted line y = 3.00 + 0.500x, while
      looking nothing like each other. Bessel's correction is measured rather
      than asserted: the divide-by-n estimator came in at 0.8005 of the true
      variance against a predicted (n-1)/n = 0.8000, and divide-by-(n-1) at
      1.0006. Under 3% contamination the standard deviation inflated 15.10x
      while the median absolute deviation moved 1.00x. The mean's breakdown
      point of zero is asserted as exact equality on the median, which does not
      move at all. Simpson's paradox uses an invented minimal table, declared
      as invented rather than dressed up with a fabricated clinical citation.
      Harness proves it can fail through an automated round-trip built into the
      script. scipy and pandas reproduce no output. Animated
      same-summary-different-data diagram. Instructor solution.
    completion: *done
  117:
    status: complete
    completed_on: '2026-08-17'
    evidence: >-
      Lesson passes content contract (Sampling and the Central Limit Theorem —
      the foundation Days 118 and 119 stand on). Sampling You Can Trust lab
      executed and re-run independently at integration (32 checks, 0 failures,
      exit 0, harness's own exit status captured); 19 reference tests pass.
      Claims re-derived at integration outside the lab's own code, on a
      different seed. The lab's centrepiece holds: measuring the spread of the
      sample mean by interquartile range from n=10 to n=1000, an exponential
      population concentrates by a factor of 9.62 against the CLT's predicted
      10, while a Cauchy population refuses entirely at a factor of 1.01 —
      averaging a thousand draws is no better than taking one, because infinite
      variance voids the theorem's condition. The sqrt(n) law reproduced with
      successive ratios of 2.02, 1.99 and 1.99 as n quadrupled. The day's
      practical claim is also checkable and checked: an accuracy of 91.4% on 500
      examples carries a binomial standard error of 1.25 percentage points, so a
      0.3-point leaderboard margin is 0.24 standard errors and demonstrates
      nothing. Sampling bias is separated from sampling error and shown not to
      shrink with n at all, and autocorrelated observations are shown to make
      the naive standard error understate the truth by a factor of about 2.4, so
      confidence grows in proportion to the error. Every sampled tolerance was
      validated across six seeds before being fixed, with observed ranges
      recorded in code. A two-spike population was reweighted to 80/20 after an
      equal-weight version proved to have near-zero skewness and failed the
      monotone-decrease check on some seeds; the reasoning is in-code rather
      than hidden. Historical attributions were grounded against the cited
      Dartmouth text rather than asserted from memory. scipy, pandas and
      matplotlib reproduce no output. Animated CLT diagram carrying the theorem
      and its condition together. Instructor solution.
    completion: *done
  118:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (Hypothesis Tests and Confidence Intervals
      — a p-value answers a far narrower question than almost everyone thinks).
      Tests You Can Defend lab executed and re-run independently at integration
      (32 checks, 0 failures, exit 0, harness's own exit status captured); 22
      reference tests pass and the untouched starter reports 1 passed and 15
      skipped, with a solved copy reaching 16. The day's centrepiece is measured
      rather than asserted: 10,000 real intervals gave 95.09% coverage, within
      three standard errors of the nominal 95%, which is the only honest way to
      teach what a confidence interval means. Multiple comparisons reproduced
      exactly at 1 - 0.95^20 = 0.6415 with a simulated 0.6435, and Bonferroni
      analytic 0.0488 against a simulated 0.0515. Peeking measured a
      false-positive rate of 0.1888 against a fixed-n control of 0.0493 —
      roughly 3.8 times nominal alpha, which is the result that should change
      how a reader runs experiments. Tolerances validated across five seeds for
      coverage, family-wise error, peeking, power and the bootstrap comparison,
      with ranges recorded. The coverage exercise deliberately uses n = 300
      because at small n the normal critical value undershoots by about a point
      against the wider t critical value; that gap was found during development
      and documented in troubleshooting rather than hidden. The agent also found
      that invoking pytest with examples and starter as separate arguments lets
      the two suites' modules collide so blank stubs pass — a real footgun,
      though no lab documents that invocation, and Day 117 was confirmed at
      integration to report 1 passed and 12 skipped on its documented path. Now
      recorded in the operating manual. scipy and statsmodels reproduce no
      output. Animated CI-coverage diagram. Instructor solution.
    completion: *done
  119:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (Analyzing an Experiment End to End —
      Week 17's capstone, on the lesson that every individual step can be
      correct and the conclusion still wrong). One Experiment, Start to Finish
      lab executed and re-run independently at integration (35 checks, 0
      failures, exit 0, harness's own exit status captured). Two seeded CSVs
      ship with the lab, 16,000 and 20,000 rows, generated by an included
      script and verified reproducible by regeneration. Dataset B's planted
      defects were re-derived at integration from the raw CSV, outside the
      lab's own code, and match to three decimals: the assignment split is
      9,600 against 10,400, so 48/52 rather than the planned 50/50, giving a
      sample-ratio-mismatch chi-square of 32.0; and the conversion lift is
      negative in every single segment — desktop -3.564pp, mobile -2.482pp,
      tablet -4.300pp — while the pooled lift is +3.888pp. That is a genuine
      Simpson's reversal rather than a contrived one, and the pipeline flags it
      instead of reporting the aggregate silently. The verdict function refuses
      to return an estimate at all once the mismatch check fails, which is the
      day's central claim made executable. Dataset A's shuffle seed was chosen
      deliberately to produce an unstable peeking path — significance crossed at
      row 4,000, reverted at 5,000, stable from 5,500 — rather than a
      monotonic one, and the choice is disclosed in code, metadata and the
      instructor solution rather than hidden. scipy, statsmodels and pandas
      reproduce no output here. Animated SRM-and-Simpson diagram. Instructor
      solution.
    completion: *done
  120:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (pandas: Series and DataFrames — a
      DataFrame is not a spreadsheet but a set of Series sharing one index, and
      the index is what makes pandas both different and surprising). Frames You
      Can Trust lab executed and re-run independently at integration through the
      lab-local venv (41 checks, 0 failures, exit 0, harness's own exit status
      captured). Requirements pin pandas 3.0.5, pyarrow 25.0.1 and numpy 2.5.2
      because this day's captured output is version-specific. Two claims in the
      author's own brief were wrong and the agent corrected both by running the
      code rather than repeating them, each confirmed independently at
      integration. First, the brief said chained assignment fails silently with
      no warning; on pandas 3.0.5 a real ChainedAssignmentError warning fires
      and names the fix. The coordinator's own first check missed this because
      it suppressed warnings — the frame is indeed left completely unchanged
      ([10, 20, 30] before and after) while the .loc form gives [10, 0, 0], but
      the reader is warned. Second, the brief's dtype-promotion exercise used
      concat, which yields object rather than float64; reindex onto a new label
      reproduces the int64-to-float64 promotion the lesson actually claims.
      Copy-on-Write is always enabled in pandas 3.x and cannot be disabled, so
      the whole SettingWithCopyWarning era is over; the default string dtype is
      str rather than object. The .loc/.iloc endpoint asymmetry is asserted so
      the difference is the thing tested rather than a coincidence, after an
      earlier framing put two agreeing slices side by side. The agent also found
      a pandas-3.0 fact not in the brief: under the new str dtype
      memory_usage(deep=True) and deep=False report identical byte counts, while
      legacy object dtype still shows a large gap. matplotlib, scipy and polars
      reproduce no output. Animated index-alignment diagram. Instructor
      solution.
    completion: *done
  121:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (Loading and Inspecting Data — read_csv is
      a type-inference engine wearing the costume of a file reader). Read It
      Right lab executed and re-run independently at integration through the
      lab-local venv (42 checks, 0 failures, exit 0, harness's own exit status
      captured); requirements pin pandas 3.0.5, pyarrow 25.0.1 and numpy 2.5.2.
      The day's two opening failures were reproduced at integration outside the
      lab's own code: pandas' default na_values contains the string NA, so
      Namibia's country code reads as a missing value, while keep_default_na
      False preserves it; and an identifier column of 00123 infers as int64 and
      becomes 123, while dtype str preserves the leading zeros. The headline
      format claim was also re-derived independently: a nullable Int64 column
      carrying a real missing value comes back from CSV as float64, while a
      Parquet round-trip preserves both Int64 and the NA exactly. The encoding
      exercise reports what actually happened on this machine — a genuine
      UnicodeDecodeError for the latin-1 byte used, not the more dramatic
      mojibake — and says both outcomes are possible. Category conversion
      measured a 12.40x memory reduction, reported as a ratio against a stated
      floor rather than as a byte count. An alt-text mismatch between em-dash
      and double-hyphen was caught by the lesson validator and fixed before
      finishing. openpyxl, matplotlib, scipy and polars reproduce no output and
      their absence was confirmed by import rather than assumed. Animated
      type-inference diagram. Instructor solution.
    completion: *done
  122:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (Selecting and Filtering — a filter is a
      claim about which rows you kept, and the rows you did not keep are your
      responsibility too). Filters That Add Up lab executed and re-run
      independently at integration through the lab-local venv (41 checks, 0
      failures, exit 0, harness's own exit status captured). The opening
      invariant is the day's habit made executable: on a frame of 8 rows with 2
      missing scores, the greater-than and less-than-or-equal filters return 3
      and 3, summing to 6 rather than 8, because NaN fails both comparisons and
      the rows vanish from both halves; handling the missing explicitly restores
      the three-way partition to 8. The author's brief was wrong about the
      headline str.contains trap and the agent measured rather than repeated it,
      confirmed independently at integration: under pandas 3.0.5's default str
      dtype a missing entry returns a clean False with a bool result and no NA
      at all, so the classic trap does NOT fire by default; it fires only on
      legacy object dtype, which returns None. Both behaviours are demonstrated
      dtype by dtype rather than one being presented as universal. Two further
      measured facts: the tilde-precedence case does not raise but silently
      computes the wrong answer, giving all-False where the correct comparison
      finds a match; and applying a mask built from a reordered copy raises a
      real UserWarning on 3.0.5, captured verbatim with its path sanitized. A
      genuine YAML bug in the day's own metadata — unescaped single quotes
      inside a single-quoted scalar — was found by running the labs validator
      and fixed rather than worked around. matplotlib, scipy and polars
      reproduce no output. Animated filter diagram. Instructor solution.
    completion: *done
  123:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (Groupby and Aggregation — split, apply,
      combine, with the discipline that the pieces must add back up to the
      whole). Groups That Reconcile lab executed and re-run independently at
      integration through the lab-local venv (13 harness checks over 20 real
      pytest assertions, 0 failures, exit 0, harness's own exit status
      captured). The opening failure is that groupby drops rows with a missing
      key by default and silently, so per-group totals come to less than the
      overall total. The lab measures a grouped sum of 1945.0 against an overall
      2115.0, a gap of 170.0 matching the missing-key rows exactly, with
      dropna=False reconciling to 2115.0. The invariant was re-derived at
      integration on a different column of the same frame and held exactly:
      grouped 65, overall 78, gap 13, equal to the missing-key total, and
      dropna=False reconciling. The observed= trap was measured at 20 rows
      against 9 for two categorical keys of 5 by 4 categories, which is the
      combinatorial explosion made concrete. The built-in agg path measured
      about 13.7 times faster than the equivalent apply on this machine, and the
      lab asserts a conservative floor of 3x rather than any millisecond figure.
      count versus size is separated because they answer different questions and
      disagree exactly where data is missing. A YAML escaping bug in the day's
      own visuals.yml was found and fixed before it could break validation.
      matplotlib, scipy and polars reproduce no output. Animated
      split-apply-combine diagram showing the shape difference between agg and
      transform. Instructor solution.
    completion: *done
  124:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (Merging and Reshaping — a join is a claim
      about cardinality, and pandas will not check it unless you ask). Joins
      That Keep Their Shape lab executed and re-run independently at
      integration through the lab-local venv (13 harness checks over 22
      assertions, 0 failures, exit 0, harness's own exit status captured). Two
      100-row frames sharing one duplicated key produce 10,000 rows — a
      Cartesian product within the key group, captured directly — and
      validate='one_to_one' raises MergeError on that same data, which is the
      day's best habit. Join-type row counts on one pair of frames: inner 3,
      left 4, right 4, outer 5. The author's brief was wrong about the
      dtype-mismatch join and the agent measured rather than repeated it,
      confirmed independently at integration: on pandas 3.0.5 merging a plain
      str key against int64 now RAISES ValueError naming both dtypes, rather
      than silently returning zero rows. The agent preserved the classic silent
      failure honestly by using a Categorical key, which still slips past that
      check and returns zero rows, and added a dedicated test proving the plain
      string case raises — two extra tests rather than a quietly dropped claim.
      It also measured that pytest with examples and starter as separate
      arguments aborts collection here with an import file mismatch and exit 2,
      where Day 117 instead returns a false green; both were reproduced at
      integration, so the operating manual now records the behaviour as
      unreliable in both directions rather than as one predictable failure.
      matplotlib, scipy and polars reproduce no output. Animated merge diagram
      showing a duplicated key fanning out. Instructor solution.
    completion: *done
  125:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (Cleaning Messy Data — cleaning is a
      sequence of irreversible decisions about data you do not have, and every
      one changes the answer). Cleaning With Receipts lab executed and re-run
      independently at integration through the lab-local venv (13 harness checks
      over 19 pytest assertions, 0 failures, exit 0, harness's own exit status
      captured); the untouched starter reports 19 skipped. The author's brief
      was WRONG about the day's centrepiece and the agent measured rather than
      repeated it, then proved the correction algebraically and confirmed it
      across 200 reseeded trials. The brief claimed mean imputation INFLATES a
      correlation because the imputed points agree with everything. It does the
      opposite: correlation strictly ATTENUATES, measured 0.745156 falling to
      0.606148 on the lab's data. Re-derived independently at integration on
      fresh data: 0.760382 falling to 0.626265. The reason is exact — an imputed
      value's deviation from the mean is zero, so it contributes nothing to the
      covariance while still counting in n, which gives r_imputed = sqrt(m/n) x
      r_complete for m complete rows out of n. That prediction was checked at
      integration and held: the standard deviation shrank by a ratio of 0.811326
      against a predicted sqrt(m/n) of 0.811326, matching to six decimal places,
      while the mean was unchanged to every digit. So the statistic a careless
      analyst would check is precisely the one imputation cannot disturb, which
      is the day's argument, and the lesson, lab, quiz, glossary and instructor
      solution all teach the measured direction and say plainly that it reverses
      the original assumption. matplotlib, scipy and scikit-learn reproduce no
      output; SimpleImputer, pyjanitor, Great Expectations and pandera are
      described from documentation only. Animated imputation diagram showing the
      distribution growing a spike while the mean line does not move. Instructor
      solution.
    completion: *done
  126:
    status: complete
    completed_on: '2026-08-19'
    evidence: >-
      Lesson passes content contract (A Reproducible Cleaning Pipeline — Week
      18's capstone, on the difference between a notebook that produced the
      right answer once and a pipeline you can hand to someone else). A Pipeline
      You Can Re-run lab executed and re-run independently at integration
      through the lab-local venv (16 harness checks over 17 pytest assertions, 0
      failures, exit 0, harness's own exit status captured); the untouched
      starter reports 17 skipped, and the lab leaves no parquet, json or csv
      artifact behind, confirmed at integration. The day opens on idempotence:
      a cleaning routine that looks correct returns a different frame the second
      time it runs, which no one notices until a scheduled job runs it nightly.
      Two design mistakes were caught by running the code rather than assumed
      away, and both are documented. Idempotence is checked through the logged
      step application rather than the full runner, because feeding a
      pipeline's output back through its own input contract raises for a reason
      that has nothing to do with idempotence — the contract requires the
      pre-cleaning dtype. And the input contract is written against pandas
      3.0.5's str extension dtype rather than object, confirmed by reading the
      error the code actually raised. Content-hash digests are captured as real
      values but documented as dependent on pandas' CSV float formatting and
      explicitly not promised portable; the tests assert hash stability across
      runs and sensitivity to a changed input byte, never a hardcoded digest,
      which is the honest form of that check. A version claim for
      DataFrame.pipe could not be confirmed from the fetched documentation, so
      the history section describes its purpose and its R magrittr provenance
      without inventing a date. matplotlib, scipy, scikit-learn, polars, pandera
      and Great Expectations reproduce no output. Animated pipeline diagram with
      contracts drawn as gates at each boundary. Instructor solution.
    completion: *done
  127:
    status: complete
    completed_on: '2026-08-20'
    evidence: >-
      Lesson passes content contract (Why We Visualize, and Choosing the Right
      Chart — a chart is an argument and the encoding is the claim). Charts That
      Answer the Question lab executed and re-run independently at integration
      (19 checks, 0 failures, exit 0, harness's own exit status captured); 17
      reference tests pass and the untouched starter reports 17 skipped. The
      hard problem of a visualisation lab is that "looks better" is not
      testable, so the lab asserts what genuinely is. The square law was
      measured rather than argued: encoding a doubled value by circle RADIUS
      renders an area ratio of 3.9503 against an analytic 4, while
      area-proportional encoding gives 1.9903 against 2 — so radius-scaled
      bubbles square every ratio in the chart. Colour deficiency was tested, not
      asserted: under the Machado 2009 severity-1.0 deuteranopia matrix a
      tab10 red/green pair collapses from a CIE76 distance of 119.7707 to
      7.3136, retaining 6.1% of its separation, while a colourblind-safe
      blue/orange pair holds 115.7010 to 116.5144. Those nine matrix
      coefficients were confirmed at integration against the authors' published
      table and match exactly. Also measured: viridis luminance rank correlation
      +1.0000 against tab10's -0.2000, showing categorical palettes destroy
      ordinal order; a data-ink ratio of 0.3669 decorated against 0.9344 plain;
      and 10,000 points painting only 6,349 distinct pixels, 36.5% invisible,
      recovering 244 grey levels under hexbin against 2 when opaque. The
      harness's own first run genuinely failed because a grep for plt.show was
      unanchored and matched a docstring promising it is never called — a check
      measuring prose rather than behaviour. It was anchored and the incident
      recorded, because it is a better failure story than the deliberate one.
      The 8% colour-deficiency prevalence could not be confirmed from any
      allowed source and is framed as a widely reported order of magnitude
      rather than attributed. A retained-distance fraction slightly above 1.0 is
      reported as measured rather than clamped to look tidy. scipy is absent so
      Spearman is written out by hand; Vega-Lite and plotly reproduce no output.
      Animated chart-choice diagram. Instructor solution.
    completion: *done
  128:
    status: complete
    completed_on: '2026-08-20'
    evidence: >-
      Lesson passes content contract (Matplotlib Fundamentals — matplotlib has
      two APIs and almost every confusing example online mixes them). Plots You
      Can Assert On lab executed and re-run independently at integration through
      the lab-local venv (34 checks, 0 failures, exit 0, harness's own exit
      status captured); 19 reference tests pass, the untouched starter reports 1
      passed and 13 skipped, a solved copy 14 passed, and the import guard was
      confirmed to prevent cross-contamination between the two suites. The day's
      central honesty point was re-derived independently at integration and
      holds: setting a log y-scale on a series containing a zero produces NO
      exception and NO warning at all, because matplotlib warns only when EVERY
      value is non-positive. The zero simply falls outside a lower axis limit
      that is strictly above zero, while the underlying artist data still
      contains it untouched — a silently vanished data point. Confirmed at
      integration that all-non-positive data raises two warnings reading "Data
      has no positive values, and therefore cannot be log-scaled", while mixed
      data raises none. The day's most practical section is that a chart is an
      object graph and therefore testable: assert on get_xlabel, get_ylim,
      lines[0].get_xydata and figure counts rather than diffing images, which is
      the technique the rest of Week 19 relies on. Nothing in this lab is
      randomly sampled, so FIELDS.md separates values exact everywhere — pixel
      arithmetic, data round-trips, figure counts — from version-specific ones
      like exact log limits and file sizes. seaborn is installed but deliberately
      unused here and deferred to Day 129; plotnine and plotly reproduce no
      output. Animated two-APIs diagram. Instructor solution.
    completion: *done
  129:
    status: complete
    completed_on: '2026-08-20'
    evidence: >-
      Lesson passes content contract (Statistical Plots with seaborn — the
      library's power and its confusion come from the same place, that it does
      statistics for you before it draws). Plots That Say What They Computed lab
      executed and re-run independently at integration three consecutive times,
      17 checks and 0 failures with exit 0 each time, harness's own exit status
      captured; 16 reference tests pass and the untouched starter reports 16
      skipped. The opening trap was reproduced independently at integration: a
      barplot's heights are group means, and for a team whose raw scores were
      90, 88, 92 and 10 the bar sits at 70.0, a value absent from its own data,
      below a team with lower individual scores because one outlier drags the
      mean. Three of the four best scores in the table sit under the shorter
      bar. The lab also establishes that seaborn's default error bar is a
      bootstrap and therefore random, so unseeded runs differ. That test was
      genuinely flaky when written as a two-run comparison — with only four
      observations per group the bootstrap resamples a small discrete space and
      two runs can coincide — and the agent found this by running the harness
      ten times rather than once, then rewrote it to draw six unseeded runs and
      assert they are not all identical, updating the brief, FIELDS.md, metadata
      and the instructor solution to match rather than leaving stale two-run
      language. seaborn 0.13.2 passes matplotlib's deprecated vert keyword to
      ax.bxp on 3.11.1, so every boxplot emits a MatplotlibDeprecationWarning;
      it is captured verbatim rather than filtered away. pyarrow was left out of
      requirements after a dry-run install showed it is not a real dependency
      here. plotnine and Altair reproduce no output. Animated estimator diagram
      showing what the bar represents and what was discarded. Instructor
      solution.
    completion: *done
  130:
    status: complete
    completed_on: '2026-08-20'
    evidence: >-
      Lesson passes content contract (Distributions and Relationships — a
      histogram is not the data, it is one picture of the data, and the bin
      width is a parameter you chose). Pictures of a Distribution lab executed
      and re-run independently at integration (16 checks, 0 failures, exit 0,
      harness's own exit status captured); 9 reference tests pass, untouched
      starter 9 skipped. The three bin-width rules disagree on identical skewed
      data — sturges 10, scott 14, Freedman-Diaconis 21 — so the number of bars
      is a choice, not a property of the sample. The centrepiece is the
      boxplot's blind spot, built as two constructed samples whose five-number
      summaries agree to within 0.3 on every quartile — bimodal [10.21, 28.02,
      40.00, 51.98, 69.79] against unimodal [10.17, 28.06, 40.00, 51.94, 69.83]
      — while a histogram at the same bin count shows two modes for one and one
      for the other. Identical boxes, different data. The author's brief was
      loose about correlation and the agent measured rather than repeating it,
      confirmed independently at integration: on a SYMMETRIC quadratic both
      Pearson and Spearman are near zero, +0.0039 and +0.0029, because Spearman
      measures monotone association and a symmetric parabola is not monotone.
      Only a fitted quadratic finds it, at R-squared 0.9962. On a monotone
      branch of the same curve Spearman does recover it at +0.9933. So no
      coefficient substitutes for the scatter, which is a sharper form of the
      day's own argument than the brief contained. scipy is absent, so pandas'
      Spearman raises ModuleNotFoundError and the lab computes it directly as
      the Pearson correlation of ranks; seaborn's kdeplot was confirmed to work
      without scipy. plotnine, Altair and scipy's gaussian_kde reproduce no
      output. Animated bin-width diagram showing a second mode appearing and
      dissolving. Instructor solution.
    completion: *done
  131:
    status: complete
    completed_on: '2026-08-20'
    evidence: >-
      Lesson passes content contract (Time Series Visualization — time is not
      just another axis, and the commonest charting mistakes throw its structure
      away). Time Told Honestly lab executed and re-run independently at
      integration after following the README's own setup (17 checks, 0 failures,
      exit 0, harness's own exit status captured); 17 reference tests pass and
      the untouched starter reports 17 skipped. Two headline claims were
      re-derived at integration outside the lab's own code and match exactly.
      Aliasing: a signal of true period 4 days sampled every 5 days produces an
      observed repeat every 4 samples, a spurious 20-day cycle that exists
      nowhere in the source — so undersampling does not merely lose detail, it
      manufactures a trend. And daylight saving: resampling hourly data to daily
      in a zone that observes it gives 23 hours on 2024-03-10, 25 on 2024-11-03
      and 24 on an ordinary day, confirmed independently. Also measured: a
      30-day trailing rolling mean peaks 14 days after the true peak while a
      centred window has zero offset, so dashboards reporting trailing means
      show events roughly half a window late; January resampled to mean 16.0,
      sum 496.0 and last 31.0 from the same 31 rows, three different charts all
      true; and constant-percentage growth is straight in log space to a
      log-difference standard deviation of 3.0e-16 against 9.5e-3 for linear
      growth. The trailing offset is asserted with a 10-to-20-day tolerance
      rather than the captured 14, because the exact figure depends on the bump
      shape and not only the window, which is documented rather than pinned.
      Every number in this lab is deterministic, unlike Day 129's bootstrap, and
      FIELDS.md says so. tzdata is documented as a fallback for platforms
      without a system IANA database but deliberately not pinned, since it was
      not needed for the captured run. seaborn was deliberately unused. Plotly
      and Bokeh reproduce no output. Animated aliasing diagram. Instructor
      solution.
    completion: *done
  132:
    status: complete
    completed_on: '2026-08-20'
    evidence: >-
      Lesson passes content contract (Visual Storytelling and Chart Honesty —
      most misleading charts are made by honest people). Charts That Cannot Lie
      To You lab executed and re-run independently at integration (59 checks, 0
      failures, exit 0, harness's own exit status captured); 42 reference tests
      pass, the untouched starter reports 22 skipped and a solved copy 22
      passed. Lie factors measured: 1.0000 for a zero-baseline bar pair against
      2.9412 for the same numbers with the axis truncated to 99-103, where the
      drawn ratio is exactly 3.0 for a 2% difference; 1.0000 for the same data
      as a line at any baseline; 4.00 for radius-encoded bubbles whose shown
      AREA ratio is 16.00; and 3D bars drawing a true ratio of 2 as 2.341 far
      and 4.204 near, against a flat control of exactly 2.000. THE AUTHOR'S
      BRIEF WAS MATHEMATICALLY WRONG and the agent refused it rather than
      implementing it. The brief asked the lab to make two uncorrelated series
      appear correlated by choosing dual-axis scalings. That is impossible:
      Pearson correlation is invariant under positive affine transforms and a
      linear dual axis is exactly such a transform. Confirmed at integration
      over 2000 random scalings, worst deviation 1.76e-15, and an inverted axis
      flips a +0.920661 to exactly -0.920661. The agent replaced it with
      something true and stronger — the SIGN is a free parameter, and the
      visual tracking gap can be driven from 0.4938 to 0.0147 on unchanged data,
      with the load-bearing control that the same widening drives a genuinely
      correlated pair to 0.0046 and an uncorrelated pair to 0.0147, so visual
      overlap is evidence of nothing. Two of the lab's datasets were
      deliberately searched for — a seed giving near-zero correlation, and
      histogram parameters where Sturges and Freedman-Diaconis genuinely
      disagree, which most settings do not — and both searches are disclosed in
      the source and FIELDS.md, with the claim narrowed to possible rather than
      typical. The brief's claim that BI tools default to non-zero baselines was
      checked against Tableau's documentation and contradicted: an Include zero
      checkbox exists and clearing it is the deviation, so the folklore was not
      repeated. JSTOR returns 200 but serves a bot challenge, so the citation
      was resolved through JSTOR's own metadata endpoint rather than guessed.
      The harness again caught a bug in itself, a plt.show check matching its own
      docstrings. Animated baseline diagram. Instructor solution.
    completion: *done
  133:
    status: complete
    completed_on: '2026-08-20'
    evidence: >-
      Lesson passes content contract (Building an EDA Report — Week 19's
      capstone, on the difference between exploring and communicating). A Report
      That Argues lab executed and re-run independently at integration through
      the lab-local venv (42 checks, 0 failures, exit 0, harness's own exit
      status captured); 9 reference tests pass and the untouched starter reports
      9 skipped. The lab builds a working report generator that enforces the
      day's thesis in code: a figure with no stated question is refused, every
      figure must be referenced, estimates must carry an interval, and 5 of 12
      candidate figures survived the so-what filter, with the 7 discarded kept
      as one-line null results so the next reader does not repeat them. Markdown
      output is byte-identical across runs. The author's brief was wrong about
      pandas Styler and the agent checked rather than assuming: .style raises
      AttributeError because it requires jinja2, which is not installed —
      confirmed independently at integration, along with nbconvert and scipy
      also being absent. Styler is described from documentation with no output
      reproduced. The most valuable finding is that the lab's own claim
      heuristic fails in BOTH directions and the lab asserts both: it passes
      "revenue doubled in every region" on data where revenue halved, and it
      REFUSES "revenue tripled in all four regions" because tripled is not on
      its word list. The agent found the false negative by writing the test and
      watching it fail, then deliberately kept it rather than adding the word,
      because a check that cannot judge truth is the point — and the content
      README forbids fixing it. Two honesty calls inside the generated report
      itself: a bootstrap interval of -11.6% to -5.1% on six observations per
      side is flagged as wide in the report's own caveats, and a +47.5% figure
      driven by one month is printed alongside the +8.0% it becomes without that
      observation, because quoting the inflated number alone would be exactly
      the Day 132 failure. PNG byte-identity is claimed only for two runs on one
      machine, since FreeType rasterisation varies with the font stack. A
      discrepancy between the flow diagram's general 4/2/1 gate split and the
      lab's own candidates, which all fall at gate 1, is stated in both the SVG
      and the prose rather than hidden. Animated figure-lifecycle diagram.
      Instructor solution.
    completion: *done
